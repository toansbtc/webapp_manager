import { NextApiRequest, NextApiResponse } from "next";
import cloudinary_config from "../config/cloudinary_config";

export default function cloudinary_uploadFile(req: NextApiRequest, res: NextApiResponse) {
}